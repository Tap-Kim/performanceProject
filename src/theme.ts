import { Theme } from "@emotion/react";
import { DefaultTheme } from "styled-components";

export const theme: DefaultTheme = {
    saleColor: '#fa622f'
}

export const etheme: Theme = {
    saleColor: '#fa622f'
}